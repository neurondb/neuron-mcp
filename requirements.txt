# NeuronMCP Python Requirements
# =============================
# Comprehensive requirements for all Python scripts in NeuronMCP
# Python 3.8+ required

# Core Database Connectivity
# Required for dataset_loader.py and database operations
psycopg2-binary>=2.9.0,<3.0.0

# Data Processing
# Required for dataset_loader.py - data manipulation and analysis
# Note: numpy is installed as a dependency of pandas
pandas>=2.0.0,<3.0.0

# HuggingFace Dataset Loading
# Required for loading datasets from HuggingFace Hub
datasets>=2.14.0,<3.0.0
huggingface-hub>=0.16.0,<1.0.0

# Cloud Storage Support (Optional but recommended)
# For S3 dataset loading
boto3>=1.28.0,<2.0.0

# HTTP/URL Support (Optional but recommended)
# For loading datasets from URLs
requests>=2.31.0,<3.0.0
urllib3>=2.0.0,<3.0.0

# Parquet File Support (Optional but recommended)
# For reading Parquet files
pyarrow>=12.0.0,<15.0.0

# Development and Testing (Optional)
# For running tests and verification scripts
# These are only needed if running test scripts
# pytest>=7.4.0,<8.0.0
# pytest-cov>=4.1.0,<5.0.0

# Note: MCP Client scripts (client/mcp_client/*.py) use only standard library
# and do not require external dependencies

